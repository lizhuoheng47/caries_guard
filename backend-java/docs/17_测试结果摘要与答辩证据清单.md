# 测试结果摘要与答辩证据清单

## 1. 当前可引用的证据类型

### 1.1 代码证据

- 控制器、服务、仓储、Flyway 迁移均在仓库内可见
- 模块边界清晰，业务路径不是伪代码

### 1.2 数据库证据

- `cg` / `cg_e2e` 都有完整 schema
- `flyway_schema_history` 已执行到 `V013`
- 默认种子数据可以支持管理员登录

### 1.3 测试证据

- 主链路真库 E2E
- analysis -> report E2E
- analysis -> report -> followup E2E
- analysis 回调幂等测试
- followup 审计与幂等测试
- dashboard 真库聚合测试

## 2. 当前最有说服力的测试文件

- `MainlineWorkflowE2ETest`
- `AnalysisToReportE2ETest`
- `AnalysisReportFollowupE2ETest`
- `AnalysisCallbackIdempotencyE2ETest`
- `FollowupTriggerIdempotencyE2ETest`
- `FollowupAuditIntegrationTest`
- `ReportExportAuditIntegrationTest`
- `DashboardOverviewIntegrationTest`
- `DashboardTrendIntegrationTest`

## 3. 答辩可直接引用的事实

1. 系统不是只有数据库设计，已经有真实接口和真实数据库测试
2. 病例状态迁移有代码规则，也有日志落库
3. AI 回调具备验签、幂等、失败回退和重试血缘
4. 报告生成会真实写 `rpt_record` 与 `med_attachment`
5. 高风险或建议复查病例会自动触发随访计划
6. dashboard 统计来自真实业务表聚合，不是模拟数据

## 4. 当前不应夸大的点

- 不能说已经接入正式 Python 推理服务
- 不能说已经完成生产级权限矩阵演示
- 不能说已经完成正式医院级 PDF 报告输出
- 不能说已经切换到真实云对象存储

## 5. 当前最稳妥的答辩表述

建议使用：

- “后端主链路已经代码化并有真库 E2E 证据”
- “工程化能力已具备基础闭环，但仍有少量部署和产品化收尾工作”
