# 模块说明与代码入口索引

更新日期：2026-04-15

## 1. `caries-system`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `AuthController` | `/api/v1/auth/login`、`/me`、`/permissions` | 登录、当前用户、当前权限 |
| `SystemAdminController` | `/api/v1/system/users` | 用户管理 |
| `SystemAdminController` | `/api/v1/system/roles` | 角色管理 |
| `SystemAdminController` | `/api/v1/system/menus` | 菜单管理 |
| `SystemAdminController` | `/api/v1/system/data-permission-rules` | 数据权限规则管理 |
| `SystemMetadataController` | `/api/v1/system/dicts`、`/configs/{configKey}` | 字典和配置查询 |
| `SystemSupportController` | `/api/v1/system/ping` | 健康检查 |
| `AuthAppService` | `login`、`currentUser`、`currentPermissions` | 认证应用服务 |
| `SystemDataPermissionRuleAppService` | `createRule`、`updateRule`、`listRules` | 数据权限规则应用服务 |

V014 已初始化 `ORG_ADMIN`、`DOCTOR`、`SCREENER`、业务菜单和数据权限规则。

## 2. `caries-patient`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `PatientController` | `createPatient`、`updatePatient`、`getPatient`、`pagePatients` | 患者管理 |
| `VisitController` | `createVisit`、`getVisit`、`pageVisits` | 就诊管理 |
| `CaseController` | `createCase`、`getCase`、`pageCases`、`saveDiagnoses`、`saveToothRecords`、`transitionStatus` | 病例管理 |
| `PatientCommandAppService` | 新增/修改患者 | 写入患者主档 |
| `PatientQueryAppService` | 患者详情/分页 | 查询患者 |
| `VisitCommandAppService` | 创建就诊 | 写入 `med_visit` |
| `CaseCommandAppService` | 创建病例、保存诊断、保存牙位、状态流转 | 病例写操作 |
| `CaseQueryAppService` | 病例详情/分页 | 病例查询 |

关键表：`pat_patient`、`med_visit`、`med_case`、`med_case_tooth_record`。

## 3. `caries-image`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `FileController` | `POST /api/v1/files/upload` | 上传附件 |
| `FileController` | `GET /api/v1/files/{attachmentId}/access-url` | 生成短时访问 URL |
| `FileController` | `GET /api/v1/files/{attachmentId}/content` | 校验签名后下载内容 |
| `CaseImageController` | `/api/v1/cases/{caseId}/images` | 病例影像创建、列表、详情 |
| `ImageQualityCheckController` | `/api/v1/images/{imageId}/quality-checks` | 影像质检保存和当前质检查询 |
| `AttachmentAppService` | `upload`、`registerExternalObject`、`createAccessUrl`、`createInternalAccessUrl`、`loadContent` | 附件应用服务；上传写 `caries-image`，外部登记用于 Python visual asset |
| `ObjectStorageService` | `store(ObjectStoreCommand)`、`load`、`delete`、`presignGetObject` | 影像模块业务端口，运行时由 `caries-integration` 适配 MinIO |
| `ObjectStoreCommand` | `bucketCode`、`keyModule`、`bizId`、`originalFileName` | 统一对象写入命令 | LOCAL_FS 兼容 provider |

## 4. `caries-analysis`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `AnalysisTaskController` | `/api/v1/analysis/tasks` | 创建、重试、详情、分页 |
| `CaseAnalysisAliasController` | `/api/v1/cases/{caseId}/analysis` | 从病例创建分析任务 |
| `CaseAnalysisAliasController` | `/api/v1/cases/{caseId}/corrections` | 从病例提交修正 |
| `AnalysisCallbackController` | `/api/v1/internal/ai/callbacks/analysis-result` | Python 回调入口 |
| `CorrectionFeedbackController` | `/api/v1/analysis/corrections` | 修正反馈入口 |
| `AnalysisTaskAppService` | `createTask`、`retryTask` | 创建任务、生成 AI 请求、发布 MQ |
| `AnalysisCallbackAppService` | `handleCallback` | 回调幂等和落库 |
| `AnalysisCallbackDomainService` | `applyCallback` | 处理 summary、visualAssets、riskAssessment |
| `CorrectionFeedbackAppService` | `submit` | 医生修正反馈和训练候选字段 |
| `RabbitAnalysisTaskEventPublisher` | `publish` | 发布 AI 分析任务消息 |

当前 AI 请求类：`AiAnalysisRequestDTO`。
当前 AI 回调类：`AiAnalysisResultCallbackCommand`。

## 5. `caries-report`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `ReportController` | `POST /api/v1/cases/{caseId}/reports` | 生成报告 |
| `ReportController` | `GET /api/v1/cases/{caseId}/reports` | 病例报告列表 |
| `ReportController` | `GET /api/v1/reports/{reportId}` | 报告详情 |
| `ReportController` | `POST /api/v1/reports/{reportId}/export` | 导出审计并返回下载信息 |
| `ReportTemplateController` | `/api/v1/report-templates` | 模板创建、修改、列表、详情 |
| `ReportAppService` | `generateReport`、`exportReport` | 报告主应用服务 |
| `ReportPdfService` | `generatePdf` | PDFBox 生成 PDF，支持中文字体候选 |
| `ReportExportResultVO` | `reportId`、`exported`、`exportLogId`、`attachmentId`、`downloadUrl`、`expireAt` | 导出返回对象 |

## 6. `caries-followup`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `FollowupPlanController` | 创建、查询、取消、关闭随访计划 | 随访计划 |
| `FollowupTaskController` | 创建、查询、改状态、分派任务 | 随访任务 |
| `FollowupRecordController` | 新增记录、按病例/任务查询记录 | 随访记录 |
| `FollowupPlanAppService` | 计划应用服务 | 写 `fup_plan` |
| `FollowupTaskAppService` | 任务应用服务 | 写 `fup_task` |
| `FollowupRecordAppService` | 记录应用服务 | 写 `fup_record` |

## 7. `caries-dashboard`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `DashboardController` | `/api/v1/dashboard/overview` | 概览 |
| `DashboardController` | `/case-status-distribution` | 病例状态分布 |
| `DashboardController` | `/risk-level-distribution` | 风险等级分布 |
| `DashboardController` | `/followup-task-summary` | 随访任务摘要 |
| `DashboardController` | `/backlog-summary` | 待办摘要 |
| `DashboardController` | `/trend` | 趋势 |
| `DashboardOpsController` | `/api/v1/dashboard/model-runtime` | 模型运行质量 |
| `DashboardStatsRepository` | `queryModelRuntime` | 按模型版本、推理耗时、不确定性、复核建议、修正反馈聚合 |
## 10. `caries-integration`

| 类 | 入口/方法 | 说明 |
| --- | --- | --- |
| `StorageProperties` | `caries.storage` | MinIO endpoint、四个 bucket、预签名过期配置 |
| `ObjectStorageClient` | `upload`、`download`、`delete`、`presignGetObject`、`presignPutObject` | 对象存储基础设施端口 |
| `MinioObjectStorageClient` | `upload`、`download`、`delete`、`presignGetObject` | MinIO SDK 实现 |
| `DefaultObjectKeyGenerator` | `generate` | 生成 `{biz-module}/{yyyy}/{MM}/{dd}/{biz-id}/{filename}` |
| `ImageObjectStorageServiceAdapter` | `store`、`load`、`presignGetObject` | 将 `caries-image` 的业务端口适配到 MinIO 客户端 |
| `ObjectStorageAutoConfig` | Spring Bean 配置 | 注册 MinIO client、storage client、key generator 和业务端口适配器 |