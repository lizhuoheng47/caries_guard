# CariesGuard 第一版实施计划

## 1. 第一版目标

第一版定位为“本科研究/竞赛级口腔全景片辅助筛查系统”，不是自动诊断产品。

系统必须满足以下底线：

- 任意上传影像均进入同一条后端分析链，不按文件名、文件哈希或内置图片返回预制结果。
- 无病灶时允许返回空结果；不支持的影像、低质量影像和低置信度结果必须拒识或进入人工复核。
- 图像模型负责识别，知识库只负责解释、引用和随访建议，不得修改病灶位置与模型类别。
- 每次结果记录模型版本、数据集版本、实现类型、阈值、耗时和是否需要复核。
- 正式环境禁止前端 Mock，禁止将启发式规则标记为训练模型。

## 2. 技术路线

```text
Vue 上传/查看
  -> Java：病例、附件、任务、权限和状态机
  -> RabbitMQ
  -> Python：影像读取与质量门控
  -> 本地 ONNX 全景片异常牙检测
  -> 置信度校准、模态检查和拒识
  -> 结构化结果（牙位、异常类型、bbox、置信度）
  -> RAG 检索审核过的知识文档并生成引用
  -> Java callback 持久化
  -> 医生复核
  -> 正式报告与随访
```

主模型采用轻量目标检测模型并导出 ONNX，运行时继续使用仓库已有的 `onnxruntime`。第一版只支持全景片，输出矩形框，不在没有像素级标注时伪造分割掩膜。云端视觉模型仅作为可关闭的实验性第二意见，不能绕过人工复核。

## 3. 开发阶段

### P0：去除演示伪结果与安全止损

- 隔离 `/ai-diagnosis` 静态演示页，旧地址重定向到真实病例上传页。
- 停用 `diagnosis.ts` 文件名特判、预制框、固定结果图和计时器假分析入口。
- 导航中的“AI 诊断分析”改为进入真实分析队列。
- 非开发构建禁止 `VITE_USE_MOCK=true`。
- 移除仓库内明文 API 密钥并要求轮换。

验收：上传文件不会再由前端直接产生诊断结论；`3_c1`、`7_c1`、`10_c1` 等名字不再影响路由或结果。

### P1：统一真实上传与任务链路

- 复用 `CasePortalView` 已有的患者、就诊、病例、附件、影像和任务创建接口。
- 增加任务状态轮询或服务端事件通知，替换前端计时器。
- 将真实分析详情、视觉资产和失败原因映射到统一页面。
- DICOM 由 Python 后端解析并生成脱敏预览；浏览器不再生成假 DICOM 图。

验收：不同文件名的同一影像结果一致；相同文件名的不同影像结果由内容决定；失败会显示明确错误码。

### P2：建立全景片真实识别基线

- 获取并审查 DENTEX 等研究数据的许可。
- 定义第一版标签：牙位、龋齿、深龋、根尖病变、阻生牙；严重度与异常类别分离。
- 按患者级别划分训练、验证、测试，建立数据审计脚本。
- 训练轻量检测模型，导出 ONNX，并新增统一 `DentalFindingAdapter`。
- 将 bbox 结果转换为现有 callback schema；暂不生成虚假 mask。

验收：保存 mAP、召回率、精确率、每图误报数和分类混淆矩阵；模型清单状态只有通过门槛后才能改为 `READY`。

### P3：可信度、拒识与人工复核

- 增加影像模态识别、模糊/曝光质量门控和分布外检查。
- 在独立验证集上确定每类阈值并进行置信度校准。
- 低置信度、质量异常、模型冲突和不支持模态强制进入复核。
- 前端明确展示模型来源、阈值、限制和复核原因。

### P4：知识库与解释

- 导入经过指导教师审核的 ICDAS/ICCMS 定义、诊疗路径及患者宣教材料。
- 第一版优先使用 `LOCAL_JSON` 和轻量检索，避免强制依赖 OpenSearch/Neo4j。
- 将结构化模型结果作为检索上下文，回填 `knowledgeVersion`、`evidenceRefs` 和 citations。
- 无有效证据时拒绝生成治疗性结论。

### P5：补齐业务壳页面

- Dashboard 接入已有 `/dashboard/*` API，删除静态统计。
- ReportPage 接入报告生成、查询和导出 API。
- 用户中心快捷按钮实现真实行为；暂不实现的按钮明确禁用。
- 增加 RAG/知识库管理入口，或修正文档中不存在的页面声明。

### P6：测试、文档与交付

- 新建 Python 单元测试、模型契约测试和端到端测试。
- 保留 Java 现有测试并增加真实 callback 场景。
- 增加“同图改名”“异图同名”“空病灶”“低质量”“不支持模态”“外部服务失败”等反作弊测试。
- 完成一键开发环境、模型下载说明、数据准备说明和验收报告。

## 4. 需要准备的外部内容

### 必需

1. 可合法用于研究的全景牙片数据及标注；首选 DENTEX 官方挑战数据。
2. 一位牙科专业教师或医生确认标签定义，并抽查训练/验证标注。
3. 可用于训练的 GPU 环境；学校服务器、教学平台免费 GPU 或短时云 GPU 均可。
4. 一套从未参与训练的本地测试影像，用于最终盲测。

### 可选

1. 云端视觉模型 API Key，仅用于实验性第二意见。
2. 云端 embedding API Key；如果没有，第一版使用本地检索。
3. 经过审核的指南 PDF/网页及其来源地址，用于知识库。

## 5. 数据目录约定

不要把含个人信息的原始影像提交到 Git。准备好数据后放入本机：

```text
data/
  raw/panoramic/
  annotations/
  external_test/
```

`data/` 应保持在 Git 忽略列表中。进入训练前统一生成脱敏后的 manifest，并记录来源、许可、患者级分组和标注版本。

## 6. 预计周期与成本

- P0-P1：约 1 周，可形成无预制结果的真实任务链。
- P2-P3：约 2-3 周，取决于数据获取和教师标注时间。
- P4-P6：约 1-2 周。
- 软件和本地 ONNX 推理可做到零授权成本；主要成本是数据审核、标注时间和少量 GPU 训练时间。

每个阶段单独验收，前一阶段不通过时不把模型或页面标记为正式可用。

## 7. 当前实施状态

P0 代码改造已完成并通过前端生产构建：旧静态诊断路由已隔离，预制诊断数据未进入生产包，生产构建禁止启用 Mock，环境文件中的明文 DashScope 密钥已移除，启发式配置已明确标记为原型模式。

P1 第一批改造已完成：前端不再提交固定 `PASS/97` 质量结果；新上传的 `PENDING` 影像由 Python 推理流水线执行质量门控；详情页按照后端状态轮询并展示真实失败码，任务未完成时隐藏诊断结果；DICOM 成功解析后生成不含 DICOM 元数据的 PNG 预览。烧录在像素中的患者文字检测以及完整容器端到端验收仍待完成。

仍需项目负责人在阿里云控制台吊销并重建曾经提交到 Git 的旧密钥；删除当前文件中的密钥不能清除 Git 历史，也不能让已经泄露的密钥自动失效。完成密钥轮换和数据准备后，继续完成 P1 剩余验收并进入 P2 模型基线训练。
